# Deep structural equality

```
deepequal.py
```

Run the Python Deepequal test right next to the implementation to see concrete examples.

Compare nested objects and arrays for structural equality — no dependencies required.

Python Deepequal relies solely on the Python standard library. There's no service to set up and nothing extra to install.